<Window x:Class="OrderManagement.UI.Windows.ForgotPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        Title="Obnovení hesla" Height="400" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid>
        <Grid.Background>
            <SolidColorBrush Color="#1a1a1a"/>
        </Grid.Background>

        <StackPanel Margin="40" VerticalAlignment="Center">
            <!-- Header -->
            <TextBlock Text="Zapomněli jste heslo?" 
                       FontSize="24"
                       FontWeight="Bold"
                       Foreground="White" 
                       Margin="10"
                       HorizontalAlignment="Center"/>

            <TextBlock Text="Zadejte svou emailovou adresu a my vám pošleme nové heslo." 
                       FontSize="12" Foreground="#b0b0b0" 
                       HorizontalAlignment="Center" TextWrapping="Wrap"
                       Margin="0,0,0,30"/>

            <!-- Email Input -->
            <xctk:WatermarkTextBox Name="txtEmail" 
                                   Watermark="Zadejte email..."
                                   Background="#333" BorderBrush="#555" BorderThickness="1"
                                   Foreground="White" FontSize="14"
                                   HorizontalContentAlignment="Left"
                                   VerticalContentAlignment="Center"
                                   Height="40" Margin="10"/>

            <!-- Buttons -->
            <Button Name="btnSendReset" Content="Odeslat nové heslo" 
                    Background="#4a9eff" BorderThickness="0"
                    Foreground="White" FontSize="14" FontWeight="SemiBold"
                    Width="200"
                    Margin="10"
                    Height="45" Cursor="Hand"
                    Click="SendReset_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" 
                                            CornerRadius="6" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" 
                                                        VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#3a8eef"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>

            <Button Name="btnCancel" Content="Zrušit" 
                    Background="#333" BorderBrush="#555" BorderThickness="1"
                    Foreground="White" FontSize="12" Height="35" Cursor="Hand"
                    Width="100"
                    Margin="10"
                    Click="Cancel_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" 
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="6" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" 
                                                        VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#444"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Window>